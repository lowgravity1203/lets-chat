<main class="main--grid-channels">
  <div class="channel--grid">
    <div class="boxes box1">
      <h1>#<%= currentChannel.name %></h1>
      <div class="search-bar">
        <input class="input-search"type="search" placeholder="🔍 Search..." class="input-field" />
      </div>
    </div>

    

    <!-- You can write a post here -->
    <div class="boxes box5">
      <form
      action="/channel/<%= currentUser._id %>/<%= currentChannel.name %>"
      method="POST"
      >
      <div class="channels-post-ctn">
          <div class="ctn1">
            <input type="text" name="text" class="post" placeholder="📝 create a post" />
          </div>
          <div class="ctn3">
            <button class="btn-post" type="submit">Send</button>
            </div>
          <div class="ctn2">
            <label> 🏷️ tag a post
            <select class="tag" name="tag">
              <% currentChannel.tag.forEach(tag => { %>
              <option value="<%= tag %>"><%= tag %></option>
              <% }) %>
            </select>
          </label>
          </div>
          <br />
      
          </form>
        </form>
      </div>
    </div>

    <!-- Posts sorted by tag will display here -->

    <%- include("../partials/box2.ejs") %>

    <!-- This will display the other channels  -->
    <div class="boxes box3">
      <div class="channels-title">#Channels</div>
      <ul>
        <a href="/channel/<%= currentUser._id %>/main">#Main</a>
        <% currentUser.interests.forEach(interest => { %> <%
        interest.name.forEach(item => { %> <% if(currentChannel.name !== item){
        %>
        <li>
          <a href="/channel/<%= currentUser._id %>/<%= item %>">#<%= item %></a>
        </li>
        <% } %> <% }) %> <% }) %>
      </ul>
    </div>

    <!-- This will display the posts -->
    <%- include("../partials/box4.ejs") %>
  </div>
</main>
